/**
 * Interface for theme with splitting to color and root classes
 */
export type ThemeWhitepaper = {
	/**
	 * Color properties
	 */
	color: string;

	/**
	 * Typography, indents, etc.
	 */
	root: string;
};
